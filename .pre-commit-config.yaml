repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.2
    hooks:
      - id: ruff-check
        args: [--fix, "--select", "I"]  # Lint + auto-fix + import sorting
        types_or: [python, pyi]

      - id: ruff-format
        types_or: [python, pyi]  # Formatter